import { ComingSoon } from "@/components/admin/ComingSoon";
import { AwardIcon } from "@/components/ui/Icon";

export default function AdminBrandsPage() {
  return (
    <ComingSoon
      icon={AwardIcon}
      title="Brands"
      description="A dedicated brand management page — for now, brands are set directly on each product."
    />
  );
}
